{
  "name": "meri-dukaan-v8",
  "version": "8.0.0",
  "description": "Meri Dukaan — Roti Business Manager PWA",
  "scripts": {
    "dev":    "npx serve . -p 3000",
    "sw":     "npx workbox-cli generateSW workbox-config.js",
    "build":  "npm run sw",
    "deploy": "npm run build && npx firebase-tools deploy"
  },
  "devDependencies": {
    "workbox-cli": "^7.3.0",
    "serve":       "^14.2.3"
  }
}
